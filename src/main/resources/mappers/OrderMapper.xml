<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  
  <mapper namespace="com.here_you_go.project.dao.IOrderDAO">
  	  	<select id="getMemberInfo" parameterType="string"
  												  resultType="com.here_you_go.project.model.MemberVO">
  		SELECT memId, memName, memHP
  		FROM member
  		WHERE memId = #{memId}	
  	</select>
  	
  	<select id="getProductInfo" parameterType="string"
  												  resultType="com.here_you_go.project.model.ProductVO">
  		SELECT *
  		FROM product
  		WHERE prdNo = #{prdNo}	
  	</select>
  	
  	 	<insert id="insertOrderInfo" parameterType="com.here_you_go.project.model.OrderInfoVO">
  		INSERT INTO order_info(ordNo, memId, ordReceiver, ordRcvPhone,
  								ordPark, ordPrice, ordStart, ordEnd,
  								petctgId, payctgId, prdNo)
  		VALUES(#{ordNo}, #{memId}, #{ordReceiver}, #{ordRcvPhone},
  					   #{ordPark}, #{ordPrice}, #{ordStart}, #{ordEnd},
  					   #{petctgId},#{payctgId}, #{prdNo})
  	</insert>
  </mapper>