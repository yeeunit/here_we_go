<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.here_you_go.project.dao.IBusinessDAO">

	<select id="businessLogin" parameterType="string" resultType="string">
		SELECT comPwd FROM company WHERE comId=#{id}
	</select>

	<insert id="joinBusiness"
		parameterType="com.here_you_go.project.model.BusinessVO">

		INSERT company(comNo, comId, comPwd, comEmail, comName,
		comTelcom,comHP)
		VALUES(#{comNo}, #{comId},
		       #{comPwd}, #{comEmail},
		       #{comName},#{comTelcom},
		       #{comHP})

	</insert>
	
	</mapper>